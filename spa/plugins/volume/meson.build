volume_sources = ['volume.c', 'plugin.c']

volumelib = shared_library('spa-volume',
                           volume_sources,
                           include_directories : inc,
                           link_with : spalib,
                           install : true)
