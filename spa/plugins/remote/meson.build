remote_sources = ['proxy.c', 'plugin.c']

remotelib = shared_library('spa-remote',
                           remote_sources,
                           include_directories : inc,
                           link_with : spalib,
                           install : true)
