
subdir('pipewire')
subdir('extensions')
subdir('daemon')
subdir('tools')
subdir('modules')
if not get_option('examples').disabled()
  subdir('examples')
endif
if not get_option('tests').disabled()
  subdir('tests')
endif

subdir('gst')
